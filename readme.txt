A random text generator using a Markov Model. Given a user-defined text file of text, generates a new file of any length, based on a key size defined by the user.

For simplicity uses rand(), which may skew the randomness to earlier random characters for any given key.
